# P3 通用底座能力设计

日期：2026-07-17
状态：第一批内核实施
范围：`backend-service/pkg/idempotency`、`backend-service/pkg/objectstorage`、后续 `app/platform/admin`

## 1. 目标

P3 通用底座能力用于补齐业务项目服务复用率最高的基础设施：

- 通知中心：站内信、邮件、短信、Webhook 通知的统一模板、渠道、发送记录和重试。
- 文件中心：租户文件、业务附件、导入导出文件和公开资源的统一元数据、权限、配额和对象存储接入。
- Webhook：面向外部系统的事件订阅、签名投递、重试、死信和审计。
- 幂等机制：为文件上传确认、Webhook 投递、通知发送、资源预留和外部回调提供统一幂等语义。

这些能力属于项目开发底座，不绑定具体业务域。业务模块只能通过明确的底座接口接入，不能直接依赖某个云厂商 SDK 或私有表结构。

## 2. 分层原则

### 2.1 通用内核层

当前第一批只落通用内核：

- `pkg/idempotency`：通用幂等执行器，按 `scope + key + fingerprint` 判断请求是否可重放或冲突。
- `pkg/objectstorage`：对象存储接口、S3-compatible 实现和本地存储实现，屏蔽 AWS S3、MinIO、Cloudflare R2、阿里云 OSS S3 协议、腾讯 COS S3 协议以及开发/私有化本地目录存储差异。

该层不依赖 `platform/admin`，可以被后续业务服务复用。

### 2.2 平台服务层

后续在 `app/platform/admin` 承载管理面：

- 文件中心管理文件元数据、租户归属、业务归属、访问策略、上传凭证、下载凭证、删除和审计。
- 通知中心管理模板、渠道、发送任务、发送记录、失败重试和渠道配置。
- Webhook 管理事件类型、订阅端点、签名密钥、投递任务、投递记录和死信重放。
- 幂等记录持久化应进入平台服务数据库，提供可清理、可审计、可按租户隔离的记录。

### 2.3 业务模块层

业务模块只使用底座能力：

- 文件上传：申请上传凭证 -> 客户端直传对象存储 -> 确认文件 -> 绑定业务资源。
- 通知发送：提交通知意图和模板变量，不直接调用邮件、短信或 Webhook SDK。
- Webhook 事件：发布领域事件，不直接投递 HTTP 请求。
- 幂等：所有外部写入接口、回调接口和异步投递使用业务幂等键。

## 3. 文件中心与多存储渠道策略

文件中心底层统一依赖 `ObjectStorageClient`，当前对象存储内核支持：

- `PutObject`：服务端上传对象。
- `DeleteObject`：删除对象。
- `PresignPutObject`：生成客户端直传 URL。
- `PresignGetObject`：生成临时下载 URL。
- `PublicURL`：生成公开访问 URL。

平台管理面通过 `StorageProviderService` 统一维护存储渠道，文件中心只解析默认渠道或文件对象上的渠道快照，不直接读取业务模块配置。当前支持两类渠道：

- `s3-compatible`：用于 AWS S3、MinIO、Cloudflare R2、阿里云 OSS S3 协议、腾讯 COS S3 协议等对象存储。
- `local`：用于开发环境、离线演示和轻量私有化部署，服务端直接写入受控基础目录。

S3-compatible 配置应包含：

- `endpoint`：存储服务地址，例如 AWS S3、MinIO、R2、OSS/COS S3 endpoint。
- `region`：签名 region，R2 或 MinIO 可使用 `auto` 或 `us-east-1`。
- `access_key` / `secret_key` / `session_token`。
- `force_path_style`：MinIO 和多数私有化部署建议开启。
- `public_base_url`：CDN 或公开下载域名，可与内部上传 endpoint 分离。
- `default_bucket`：默认桶，业务仍可按租户或模块拆 bucket。

Local 配置应包含：

- `local_base_path`：对象写入根目录，必须由平台管理员显式配置。
- `public_base_url`：可选；配置后公开文件和下载链接可生成稳定访问地址。
- `default_bucket`：本地目录下的逻辑 bucket，保持与 S3-compatible 元数据模型一致。

设计约束：

- 同一时刻只能有一个默认启用渠道，租户文件创建上传会话时使用该默认渠道。
- 文件对象必须保存 `provider_id`、`provider_code` 和 `storage_provider` 快照，默认渠道切换不影响历史文件下载和删除。
- 删除存储渠道前必须检查是否仍被文件对象引用，避免历史对象失去解析能力。
- 本地存储禁止路径穿越；客户端直传仅适用于 S3-compatible，本地存储走服务端上传入口。

文件中心表结构：

- `file_objects`：文件元数据、租户、业务类型、业务 ID、bucket、object key、大小、hash、content type、状态、可见性。
- `storage_providers`：存储渠道配置、类型、默认状态、健康状态、公开访问地址、默认 bucket 和本地路径。
- `file_upload_sessions`：上传会话、预签名凭证、过期时间、幂等键、确认状态。
- `file_access_logs`：下载、预览、删除等访问审计。

## 4. 通知中心策略

通知中心不直接等同短信配置。建议拆分：

- 通知模板：模板编码、标题、内容、变量 schema、语言。
- 通知渠道：站内信、邮件、短信、Webhook。
- 通知任务：接收人、渠道、模板变量、优先级、计划发送时间。
- 通知记录：发送状态、渠道响应、失败原因、重试次数。

发送必须异步化，复用 `AsyncTask` 作为任务执行底座。每次发送使用幂等键，避免重试导致重复通知。

当前通知中心 MVP 已落地站内信：

- `notification_templates`：按租户保存模板编码、名称、渠道、标题模板、内容模板、变量 schema、语言和启停状态。
- `notification_messages`：按租户和接收用户保存站内信记录、已读未读状态、业务归属、发送人快照和阅读时间。
- `NotificationService`：提供模板管理、站内信发送、通知记录、我的通知、未读数、单条已读和批量已读接口。
- `notification.in_app.send`：复用 `AsyncTask` 的 notification 队列执行站内信生成；任务 payload 显式携带 `tenant_id`，worker 不依赖登录态上下文。
- 管理后台 `/system/notification`：同一入口内提供模板和通知记录两个视图，并支持发送站内信。

邮件、短信、Webhook 渠道先保留契约和枚举扩展位，后续接入真实渠道时必须补充渠道响应、失败原因、重试次数和供应商配置审计。

## 5. Webhook 策略

Webhook 是事件中心的外部投递面，不是任意 HTTP 调用工具。必须满足：

- 事件类型白名单。
- 订阅端点归属租户。
- 每个订阅独立签名密钥。
- 投递请求携带事件 ID、时间戳、签名和重试次数。
- 2xx 视为成功；非 2xx 或网络错误进入指数退避重试。
- 达到最大重试次数进入死信，支持人工重放。

Webhook 投递记录需要持久化，并使用 `scope=tenant:<id>:webhook:<subscription_id>` 与事件 ID 作为幂等键。

## 6. 幂等策略

通用幂等由三个字段决定：

- `scope`：隔离范围，例如 `tenant:100:file`、`tenant:100:webhook:9`、`platform:quota`。
- `key`：业务幂等键，例如上传会话 ID、外部回调 ID、事件 ID。
- `fingerprint`：请求语义摘要，同一个 key 被不同请求体复用时必须返回冲突。

处理规则：

1. 第一次请求执行真实处理并保存结果。
2. 相同 `scope + key + fingerprint` 再次请求直接回放第一次结果。
3. 相同 `scope + key` 但 `fingerprint` 不同，返回幂等冲突。
4. 处理函数失败时不保存成功结果，允许重试。
5. 持久化实现必须支持过期清理，避免幂等记录无限增长。

## 7. 实施顺序

1. 第一批：通用幂等内核、S3-compatible 对象存储内核、架构文档。
2. 第二批：文件中心 proto、Ent schema、上传会话、文件元数据、S3 预签名 API、mock 数据和前端页面。
3. 第三批：通知中心 proto、模板/渠道/记录、异步发送任务、站内信 MVP。
4. 第四批：Webhook 事件订阅、投递任务、签名验证、重试和死信。
5. 第五批：所有 P3 能力接入资源额度、操作日志、租户套餐能力和前端权限菜单。

## 8. 实施记录

### 8.1 第一批：通用内核

已完成：

- `pkg/idempotency` 通用幂等执行器。
- `pkg/objectstorage` S3-compatible 和 Local 对象存储抽象。

- `GOCACHE=/private/tmp/avmc-go-cache go test ./pkg/idempotency ./pkg/objectstorage`

第一批不修改 `proto` 和生成代码，不引入第三方云厂商 SDK。S3 签名逻辑由底座内核实现，Local 实现仅依赖标准库文件系统能力；后续如需要 Multipart Upload 或高级生命周期管理，再评估引入官方 SDK 或 MinIO SDK。

### 8.2 第二批：文件中心后端 MVP

已完成：

- `core/service/v1/file.proto` 和 `platform/admin/v1/i_file_center.proto`。
- `FileObject` Ent schema，记录租户文件元数据、存储 provider、bucket、object key、业务归属、可见性、上传过期时间和确认状态。
- `FileCenterService` 后端接口：
  - 创建上传会话。
  - 本地存储服务端上传文件内容。
  - 确认上传。
  - 文件列表和详情。
  - 文件访问日志列表。
  - 生成下载预签名 URL。
  - 删除文件并软删除元数据。
- `StorageProviderService` 后端接口：
  - 创建、编辑、删除、查询和分页检索存储渠道。
  - 设置默认存储渠道。
  - 测试渠道连通性并记录健康状态。
- `storage_providers` Ent schema，记录渠道类型、S3-compatible 配置、本地目录、默认 bucket、默认状态和健康状态。
- 文件对象新增 `provider_id` 和 `provider_code`，同时保留 `storage_provider` 字符串快照；历史文件优先按快照渠道解析，默认渠道只影响新上传会话。
- 通过平台存储渠道配置映射对象存储客户端；当数据库未配置渠道时，仍可兼容既有 `conf.OSS.MinIO` 配置作为旧环境回退。
- 文件中心操作纳入后端权限保护，并保持租户数据面分类，不归入平台控制面。
- 存储渠道管理纳入平台控制面权限，普通租户不能配置全局存储渠道。
- 文件中心接入资源额度闭环：
  - 创建上传会话前检查 `files` 和 `storage.bytes`。
  - 上传确认后使用 `file:confirm:<id>:files` 和 `file:confirm:<id>:storage.bytes` 幂等键占用额度。
  - 删除文件后使用 `file:delete:<id>:files` 和 `file:delete:<id>:storage.bytes` 幂等键释放额度。
- 文件访问日志接入数据面：下载和删除动作写入 `file_access_logs`，列表接口按租户隔离并进入文件中心按钮权限。
- Mock 数据接入文件中心：
  - 新增 `/system/file-center` 页面菜单和列表、详情、上传、确认、下载、删除按钮权限。
  - `demo-full-admin` 套餐增加 `platform.admin.file.manage`、`file_center` 功能开关、`files` 和 `storage.bytes` 资源额度。
  - 租户 1 初始化 4 条文件对象，覆盖合同、导入文件、公开 Logo 和待确认上传；租户 2 初始化 2 条文件对象，覆盖发票和归档文件。
  - Mock 自检输出新增 `tenant 1 file objects` 和 `tenant 2 file objects`，用于验证后台文件中心可见数据和跨租户隔离数据。
- Mock 数据接入存储渠道管理：
  - 新增 `/system/storage-provider` 页面菜单和列表、新增、编辑、删除、设为默认、测试连接按钮权限。
  - 初始化默认本地存储渠道 `mock-local-default` 和示例 S3-compatible 渠道 `mock-s3-compatible`。
  - Mock 自检输出新增 `storage providers count=2`。
- 管理后台新增存储渠道页面，支持列表、筛选、新增、编辑、删除、设为默认和测试连接；密钥字段编辑时不回显明文，空值不覆盖原密钥。
- 管理后台新增文件中心页面，支持文件列表、详情抽屉、下载、删除和访问记录查看。

验证：

- `GOCACHE=/private/tmp/avmc-go-cache go test ./app/platform/admin/internal/biz ./app/platform/admin/internal/data ./app/platform/admin/internal/service ./app/platform/admin/internal/server ./app/platform/admin/internal/authzpolicy ./app/platform/admin/cmd/server ./pkg/objectstorage`
- `GOCACHE=/private/tmp/avmc-go-cache go test ./app/platform/admin/internal/authzpolicy ./app/platform/admin/cmd/mock`
- `GOCACHE=/private/tmp/avmc-go-cache go run ./cmd/mock -conf ./configs -migrate`
- `GOCACHE=/private/tmp/avmc-go-cache go run ./cmd/mock -conf ./configs`，重复执行后校验 `tenant 1 file objects count=4`、`tenant 2 file objects count=2`
- `pnpm -F @vben/web-antd-admin run typecheck`
- `pnpm -F @vben/web-antd-admin run build`

未完成：

- Multipart Upload、对象扫描、访问日志报表和高级审计、CDN 私有签名和本地文件下载代理。

### 8.3 第三批：通知中心 MVP

已完成：

- 新增 `core/service/v1/notification.proto` 和 `platform/admin/v1/i_notification.proto`。
- 新增 `NotificationTemplate` 和 `NotificationMessage` Ent schema。
- 后端 `NotificationUsecase` 支持模板管理、站内信发送任务、通知记录、我的通知、未读数、单条已读和批量已读。
- `AsyncTask` 注册 `notification.in_app.send` handler，按模板和变量渲染站内信。
- 通知接口纳入后端权限保护；我的通知、未读数和已读接口归入认证自服务能力。
- Mock 数据新增 `/system/notification` 菜单、模板/记录/发送按钮权限、通知模板、通知消息和 notification 队列任务。
- 管理后台新增通知中心页面，支持模板列表、新增、编辑、删除、通知记录列表和发送站内信。

验证：

- `GOCACHE=/private/tmp/avmc-go-cache go test ./app/platform/admin/internal/biz ./app/platform/admin/internal/data ./app/platform/admin/internal/service ./app/platform/admin/internal/authzpolicy ./app/platform/admin/internal/server ./app/platform/admin/cmd/server ./app/platform/admin/cmd/mock`
- `GOCACHE=/private/tmp/avmc-go-cache go test ./...`
- `PATH=/Users/jayden/go/bin:$PATH buf build`
- `PATH=/Users/jayden/go/bin:$PATH ./scripts/check-buf-lint-baseline.sh`
- `GOCACHE=/private/tmp/avmc-go-cache go run ./cmd/mock -conf ./configs -migrate`
- `GOCACHE=/private/tmp/avmc-go-cache go run ./cmd/mock -conf ./configs`
- `pnpm -F @vben/web-antd-admin run typecheck`
- `pnpm -F @vben/web-antd-admin run build`

未完成：

- 邮件、短信、Webhook 真实发送渠道。
- 用户通知偏好、租户通知配置和平台公告。
- 渠道响应详情、失败原因统计和通知重试报表。
