# 订阅计费设计

日期：2026-07-22
状态：📋 设计阶段
优先级：🟡 P2

---

## 一、计费模型

```mermaid
erDiagram
    TENANT ||--o{ SUBSCRIPTION : "有订阅"
    PRODUCT_PLAN ||--o{ SUBSCRIPTION : "选择方案"
    SUBSCRIPTION ||--o{ QUOTA_USAGE : "用量记录"
    SUBSCRIPTION ||--o{ BILL : "产生账单"
    BILL ||--o{ PAYMENT : "有支付"

    SUBSCRIPTION {
        string subscription_id PK
        string tenant_id FK "客户"
        string product_code FK "产品"
        string plan_id FK "方案"
        string cycle "monthly|yearly"
        date start_date "开始日期"
        date end_date "到期日期"
        string status "active|expired|cancelled"
        decimal base_price "基础价格"
    }

    QUOTA_USAGE {
        string usage_id PK
        string subscription_id FK
        string dimension "配额维度"
        int used "已用量"
        int limit_amount "限额"
        string period "计费周期 (2026-07)"
    }

    BILL {
        string bill_id PK
        string tenant_id FK
        string period "账期"
        string product_code "产品线"
        decimal base_charge "基础费用"
        decimal overage_charge "超额费用"
        decimal total "合计"
        string status "pending|paid|overdue"
    }
```

---

## 二、配额模型详解

### 软限制 vs 硬限制

```mermaid
flowchart TB
    Request["产品服务上报用量<br/>POST /billing/usage/report"]

    Request --> Check{"检查配额"}
    Check -->|"余量 > 0"| Allow["allowed: true<br/>正常扣减"]
    Check -->|"余量 = 0 (soft)"| Soft["allowed: true<br/>允许超额<br/>返回超额费用估算"]
    Check -->|"余量 = 0 (hard)"| Hard["allowed: false<br/>403 QUOTA_EXCEEDED<br/>拒绝操作"]

    Soft --> Record["记录超额用量<br/>下个账期结算"]
    Allow --> Done["操作完成"]
    Record --> Done
```

### 配额维度示例

```text
GEO Engine Pro 套餐:
  · article_count:  30 篇/月  (hard, 超额单价 ¥120/篇)
  · brand_case:     10 个/月  (soft)
  · storage_gb:     50 GB     (soft)

CRM Starter 套餐:
  · customer_count: 500 个    (soft)
  · pipeline_count: 3 个      (hard, 超额单价 ¥50/个)
  · user_seat:      10 个     (hard)
```

**计费引擎不关心 "article_count" 是什么** — 它只处理维度码、限额、超额策略和单价。这是产品线与计费引擎的解耦方式。

---

## 三、用量上报接口

```text
POST /api/billing/usage/report
  Header: X-Tenant-Id: T_A
  Body: {
    product_code: "GEO",
    dimension: "article_count",
    amount: 1,
    action: "increment"
  }

  响应 200 (有余量):
  {
    allowed: true,
    quota: {
      dimension: "article_count",
      used: 29, limit: 30,
      remaining: 1,
      usage_percent: 96.7,
      warning: "配额即将耗尽"
    }
  }

  响应 200 (超额-软限制):
  {
    allowed: true,
    quota: { ... },
    overage: {
      overage_count: 2,
      overage_unit_price: 120,
      estimated_charge: 240
    },
    warning: "已超出配额，将产生超额费用"
  }

  响应 403 (超额-硬限制):
  {
    allowed: false,
    reason: "配额已耗尽，请充值",
    action_required: "recharge"
  }
```

---

## 四、续费流程

```mermaid
flowchart TB
    Active["订阅生效中"] --> T15["到期前 15 天<br/>发送续费提醒"]
    T15 --> T7["到期前 7 天<br/>第二次提醒"]
    T7 --> T3["到期前 3 天<br/>第三次提醒"]

    T3 --> Pay{"客户续费?"}
    Pay -->|"是"| Renew["续费成功<br/>end_date 延长"]
    Pay -->|"否"| Expire["到期<br/>status = expired"]

    Expire --> Grace["30 天宽限期<br/>数据保留，功能受限"]
    Grace --> PayGrace{"宽限期内续费?"}
    PayGrace -->|"是"| Renew
    PayGrace -->|"否"| Cleanup["30 天后数据清理"]
```

---

## 五、当前工程基线 vs 目标

| 能力 | 当前状态 | P2 目标 |
|------|---------|---------|
| 套餐版本化 | ✅ 已实现 | 关联产品定价 |
| 资源配额框架 | ✅ 已实现 | 关联订阅周期 |
| 用量上报 | ❌ | API 标准化 |
| 账单生成 | ❌ | 月度自动生成 |
| 支付集成 | ❌ | 微信/支付宝 |
| 续费管理 | ❌ | 自动提醒+宽限 |
| 发票管理 | ❌ | 电子发票 |
| 优惠/折扣 | ❌ | 优惠券引擎 |
