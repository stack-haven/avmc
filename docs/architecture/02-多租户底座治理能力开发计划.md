# 多租户底座治理能力开发计划

日期：2026-06-27  
状态：实施中  
范围：项目开发底座，不包含业务服务

## 1. 架构目标

多租户底座需要同时提供两类能力：

- 平台控制面：租户生命周期、业务套餐、平台配置、审计监管和资源治理。
- 租户数据面：组织、成员、角色、菜单、字典、租户配置及租户业务数据隔离。

所有新增项目服务必须复用租户上下文、权限、审计和配置机制，不得自行实现另一套租户识别或操作日志。

## 2. 架构原则

1. 后端强制隔离：租户边界由认证上下文、Ent policy 和 repository 查询共同保证。
2. 契约优先：API 先修改 `backend-service/proto`，再生成传输代码并实现 Kratos 分层。
3. 控制面显式授权：平台跨租户操作必须使用平台身份和显式目标租户，不能依赖普通租户管理员权限。
4. 审计不可变：操作日志只允许追加和查询，不提供修改、删除接口。
5. 配置可追踪：字典、参数、套餐等配置变更必须具备版本、缓存失效和审计记录。
6. 业务无关：底座只提供通用治理能力，不承载版本、发布、反馈等项目服务业务。

## 3. 能力路线图

### P0 基础治理

- 数据字典
- 操作审计日志
- 登录与会话安全
- 租户生命周期和初始化编排
- 租户上下文与隔离自动化测试
- 业务套餐版本化
- 平台身份与租户身份安全边界

### P1 平台管理

- 参数配置中心（首期已完成）
- 数据权限（用户与组织成员首期已实施）
- 租户管理员安全边界
- 组织成员生命周期
- 通知中心
- 文件与对象存储
- 异步任务中心（首期执行内核已完成）

### P2 规模化

- 租户配额与限流
- 开放接口与应用凭证
- Webhook 事件中心
- 租户品牌和域名
- 国际化与时区
- 可观测性
- 数据导入导出和隐私治理

## 4. 第一阶段实施范围

### 4.1 数据字典

聚合：

- `DictionaryType`：字典类型，租户内 `code` 唯一。
- `DictionaryItem`：字典项，在字典类型内 `value` 唯一。

行为：

- 租户管理员维护本租户字典类型和字典项。
- 字典项支持标签、值、排序、颜色、状态和备注。
- 禁用字典类型后，其字典项不应作为有效业务配置使用。
- 已被业务引用的字典项删除保护在建立统一引用注册机制后实现。
- 平台字典模板采用“发布版本 -> 租户订阅/覆盖”模型，在平台身份模型完成后实现，首期不使用 `tenant_id = 0` 绕过隔离。

### 4.2 操作审计

聚合：

- `OperationLog`：追加写入的审计事件。

记录内容：

- 租户、操作人、模块、动作、资源类型和资源 ID。
- HTTP/gRPC operation、请求方法和路径。
- 请求参数摘要、变更前后差异。
- IP、User-Agent、Trace ID、执行结果、耗时和错误信息。

约束：

- 对外只开放列表和详情查询。
- 写入由底座内部 `OperationAuditRecorder` 统一完成。
- 密码、令牌、密钥、验证码等字段进入持久化前必须脱敏。
- 已提供记录器、查询闭环和 HTTP/gRPC 自动审计 middleware；中间件只覆盖受保护的写操作，读取操作不产生审计记录。

## 5. 分阶段实施

### 阶段 A：模型与基础闭环

- 字典和审计 Proto 契约。
- Ent schema、repository、usecase、service、Wire 和服务注册。
- 后端权限操作清单。
- 字典管理页面和审计查询页面。
- 中英文文案与开发模拟数据。

### 阶段 B：平台控制面与自动审计

- 平台身份与租户身份分离（已完成首期：平台租户内部标记、签名令牌声明、Casbin 策略拆分和统一中间件守门）。
- 平台字典模板版本、发布、订阅和租户覆盖。
- 平台字典模板发布、订阅和覆盖。
- 敏感字段注册表、变更差异提取和异步批量写入。
- 登录日志、会话管理和强制下线。

### 阶段 C：租户生命周期

- 租户状态机（已实现首期状态迁移、认证拦截和会话撤销）。
- 创建租户初始化编排和补偿（已实现单库原子事务；跨服务补偿待项目服务拆分后引入）。
- 套餐版本、升级、降级和回滚（不可变版本、立即发布、租户固定/跟随和回滚已完成；定时生效等待统一任务中心）。
- 租户停用后的会话失效与缓存刷新。

### 2026-06-29 租户生命周期与原子开通

- 新增独立租户生命周期：待开通、正常、暂停、到期和注销，保留通用 `status` 作为过渡兼容字段。
- 新增生命周期状态迁移接口；注销为终态，旧启停接口统一映射为暂停和恢复，不能绕过状态机。
- 创建租户升级为原子开通事务，同时创建套餐绑定、根部门、租户管理员角色和初始管理员。
- 管理员角色菜单严格取已启用套餐中已启用菜单的并集；停用套餐不能用于新租户开通。
- 初始密码在 biz 层执行统一强度校验和哈希，明文不进入 data 层或响应。
- 认证同时检查租户通用状态、生命周期、软删除和到期时间；离开正常状态后撤销租户全部会话。
- 前端新增流程升级为“租户信息 -> 业务套餐 -> 初始管理员”三步，编辑流程仍保持两步。
- 后端完整测试和前端 `vue-tsc` 类型检查通过；本地数据库迁移与真实 HTTP 联调因执行环境额度限制未能在本轮完成。

### 2026-06-29 业务套餐版本化

- 新增不可变 `MenuPermissionGroupVersion` 权限快照，套餐引用当前发布版本。
- 创建套餐自动生成版本 1；兼容编辑入口在菜单变化时自动发布下一版本，仅修改元数据不产生版本。
- 新增版本历史、显式发布和回滚接口；回滚复制历史权限并生成更高版本，不修改历史快照。
- 租户套餐绑定新增 `version_id` 和 `auto_upgrade`，支持跟随最新版或固定任一历史版本。
- 发布时只推进自动跟随绑定，固定版本租户权限保持不变；保存相同套餐选择不会重置固定策略。
- 最终有效菜单优先读取绑定版本快照，历史未迁移绑定回退到套餐当前菜单。
- mock 初始化可为历史套餐幂等生成版本 1，并为历史租户绑定补齐当前版本。
- 前端套餐列表增加当前版本、版本历史、发布和回滚；租户套餐绑定页增加跟随/固定版本交互。
- 后端增加版本发布、固定、回滚和事务回滚测试；完整 `go test ./pkg/auth ./app/platform/admin/...` 通过。
- 前端 `pnpm --filter @vben/web-antd-admin typecheck` 通过。
- 定时生效不使用单进程定时器；待统一异步任务中心具备持久化、抢占锁、重试和监控后开放。
- 定向 `buf build` 通过；`buf lint` 仍因既有 core/platform 双 service 复用请求响应消息规则失败，本期未进行破坏性契约重命名。

### 2026-06-30 平台与租户身份安全边界

- 确认并修复普通租户 `super_admin` 可获得租户、套餐和全局菜单控制面策略的越权风险。
- 租户表新增仅供内部初始化维护的 `is_platform` 标记，不进入公开租户 API。
- 登录、刷新和令牌轮换均签入可信 `platform_operator` 声明，声明来源只取数据库。
- 新增统一控制面中间件；控制面请求除 Casbin 放行外，还必须具备签名平台身份。
- Casbin 拆分租户管理员和平台管理员策略；同步时先清理完整旧策略，防止历史平台授权残留。
- mock 只将租户 1 初始化为平台租户，租户 2 及其他演示租户均为业务租户。
- 新增租户隔离测试矩阵，覆盖用户、角色、部门、岗位、项目、字典、操作日志和登录日志的跨租户读写删除。
- 测试发现字典项可直接引用其他租户字典类型，已增加 Ent 写入钩子，在存储层拒绝跨租户外键。
- `go test ./pkg/auth ./app/platform/admin/...` 通过。

### 2026-06-30 多租户参数配置中心

- 新增平台参数定义和租户参数覆盖模型，支持字符串、整数、布尔值和 JSON。
- 新增平台定义管理、当前租户自助覆盖和平台指定租户管理接口。
- 最终参数由后端统一解析并标记平台默认或租户覆盖来源。
- 参数定义变更递增全局版本，租户覆盖变更递增租户版本，Redis 解析缓存使用双版本键立即失效。
- Redis 读取或写入异常采用数据库降级，不阻断参数主流程。
- 参数类型变更、定义删除、禁止覆盖等状态冲突均有后端保护。
- 密码、Secret、API Key、私钥和 Token 类键禁止进入普通参数中心。
- 前端参数页按权限展示平台定义和当前租户参数，普通租户不会调用控制面接口。
- mock 增加 8 个参数定义和两个租户的差异化覆盖值。
- 后端参数校验、默认/覆盖/重置、禁止覆盖和租户隔离测试通过；前端类型检查通过。

### 2026-06-30 统一异步任务中心

- 新增持久化 `AsyncTask` 模型，平台任务使用空租户归属，租户任务保存真实租户 ID。
- 任务只能由已注册后端 handler 入队，管理 API 不开放任意任务创建和原始载荷读取。
- 支持幂等键、计划时间、优先级、最大执行次数和脱敏载荷摘要。
- 多实例 worker 使用数据库条件更新抢占租约，旧 worker 不能在租约失效后覆盖新执行结果。
- 失败任务使用指数退避自动重试，达到上限后进入失败状态；平台管理员可人工重试。
- handler panic 自动恢复并进入失败重试流程，不影响 HTTP/gRPC 服务进程。
- worker 接入 Kratos 服务生命周期，首期轮询 `default` 和 `maintenance` 队列。
- 内置每日终态任务保留期清理，默认保留 30 天。
- 新增平台任务监控页面，支持筛选、取消待执行任务和重试失败任务。
- 后端测试覆盖幂等、独占抢占、租约接管、重试、取消、清理和 panic 恢复。

### 2026-07-01 异步任务中心真实环境验收

- 在本地 MySQL 完成 Ent Schema 迁移并初始化异步任务演示数据。
- Kratos 服务启动后自动创建每日维护任务，worker 完成抢占、执行和结果落库。
- 平台管理员任务列表、详情、取消和重试接口通过真实 HTTP 验证。
- 失败任务人工重试后由 worker 自动执行成功；已取消任务再次取消返回 409 状态冲突。
- 普通业务租户访问任务中心返回 403，平台控制面边界生效。
- 服务停止时 worker 正常退出，无遗留执行进程。

### 2026-07-02 租户内数据权限首期

- 将角色已有但未生效的 `data_scope` 落实为用户成员查询约束。
- 用户新增主部门，角色新增自定义数据范围部门集合，关联均校验当前租户归属。
- 支持全部、本人、本部门、本部门及下级、自定义部门五种范围，多角色按并集合并。
- 新角色默认仅本人；租户管理员显式拥有租户内全部数据。
- 用户列表、简单列表、详情和计数共享范围解析，防止分页总数或直接 ID 查询泄漏。
- 部门移动同步重写后代祖先链，保证部门树范围长期正确。
- 修复用户保存后角色关系未回读，以及管理员启停未同步 Casbin 的一致性问题。
- 前端增加用户主部门和角色数据范围配置。
- 自动化检查、真实 MySQL 迁移和幂等 Mock 初始化已通过。
- 真实 HTTP 已验证租户登录、租户套餐关联回显、套餐菜单回显、角色授权回显和用户部门/角色回显。
- 联调发现租户 2 普通成员误绑管理员角色，已增加独立普通成员角色并重新初始化数据。
- 最后管理员保护自动化测试和真实 HTTP 验证均通过：租户仅剩一名启用管理员时，停用操作返回 `409 LAST_TENANT_ADMIN_REQUIRED`，用户状态保持启用。
- 建立 Buf lint 无新增债务基线；当前历史违规按文件和规则计数固化，新增违规将使统一验收失败，旧契约债务按后续迭代逐步清理。
- 新增管理后台 Playwright 真实后端验收，测试启动时关闭 Nitro Mock，覆盖登录、租户、业务套餐、角色、用户、参数和异步任务页面。
- 联调修复管理表格将数字偏移量作为 `page_token` 时后端解析失败的问题；统一分页层兼容数字偏移和 AIP 不透明令牌，并覆盖正数、负数边界测试。
- 前端依赖、类型检查、生产构建和 Playwright 真实后端验收均已通过；联调同时修复权限码/动态菜单响应解包、菜单查询参数类型适配和 E2E 误连远端 Mock 的问题。

### 阶段 D：治理扩展

- 参数中心、数据权限、配额、通知、文件和任务中心。
- 开放接口、Webhook、品牌域名、可观测性和隐私治理。

## 6. 第一阶段验收标准

- 同一租户不能创建重复字典编码。
- 不同租户可使用相同字典编码，且互相不可见。
- 字典项只能属于当前租户的字典类型。
- 审计日志不存在更新和删除 API。
- 普通租户只能查询本租户审计日志。
- 审计记录中的敏感字段已脱敏。
- 后端生成、编译和不依赖数据库的单元测试通过。
- 前端类型检查通过。

## 7. 联调阶段

数据库启动后统一执行：

1. Ent schema 迁移。
2. 初始化字典菜单、按钮权限和演示数据。
3. 同步 Casbin 策略。
4. 验证两个租户之间的字典和审计数据隔离。
5. 验证字典 CRUD、审计筛选和分页。
6. 验证写操作审计、脱敏、Trace ID 和失败记录。

## 8. 实施记录

### 2026-06-27

- 完成数据字典和操作审计 Proto 契约及本地生成配置。
- 完成 `DictionaryType`、`DictionaryItem`、`OperationLog` Ent 模型和租户隐私规则。
- 完成 dictionary/audit 的 Kratos service、biz、data、Wire 和 HTTP/gRPC 注册。
- 操作日志对外只提供列表和详情，不提供修改、删除 API。
- 字典写操作已接入审计记录器，持久化前对密码、令牌、密钥和验证码字段脱敏。
- 完成数据字典管理页、字典项维护抽屉和操作审计只读列表。
- 完成中英文文案、前端路由、Casbin 受保护操作和 mock 菜单定义。
- 数据库未启动，本次未执行迁移和 HTTP 联调；保留在第 7 节统一执行。

### 2026-06-28

- 对本地 `platform_system` 执行 Ent schema 迁移，成功创建字典类型、字典项和操作日志表。
- 重新执行幂等 mock 初始化，验证 2 个租户、12 个系统菜单、2 个业务套餐和 2 条租户套餐绑定。
- Casbin 策略和租户菜单缓存版本已随 mock 初始化同步。
- 通过真实 HTTP 完成租户 1 登录、字典创建、字典项创建、字典查询和审计查询。
- 通过租户 2 账号验证：不能读取租户 1 字典；不同租户允许使用相同字典编码；向租户 1 字典类型写入字典项返回 404。
- 联调发现通用 `code` 被误判为验证码并脱敏，已调整为仅脱敏 `captcha_code`、`sms_code`、`verification_code` 等明确敏感键，并补充单元测试。
- 字典写操作已真实生成租户内操作审计记录，字典编码在审计摘要中正常保留。

### 2026-06-28 大数据量演示数据

- 将 `cmd/mock` 扩展为幂等的大数据量演示数据生成器，重复执行不会重复追加稳定 mock 记录。
- 租户扩展为 8 个，其中包含完整权限、基础权限、行业示例和停用状态租户。
- 租户 1 用户扩展为 29 个、部门 8 个、项目 19 个；租户 2 用户扩展为 9 个、项目 7 个。
- 生成 15 个字典类型和 68 个字典项，覆盖订单、支付、通知、工单、合同、设备和数据敏感级别等后台展示场景。
- 生成 160 条稳定 mock 操作日志，并保留真实联调产生的操作日志；日志覆盖 8 个模块、6 类动作、成功/失败结果、IP、Trace ID 和耗时。
- 连续执行两次 mock 后计数完全一致，幂等性验证通过。
- 真实 HTTP 验证通过字典分页、第二页令牌、编码筛选、审计模块筛选和租户隔离。
- 联调发现 `project_members` 被 Ent 生成成一对多关系，导致一个用户只能属于一个项目；已增加 `User.projects` 反向边并重新生成多对多中间表。
- 增加同一用户同时属于两个项目的回归测试，真实数据库迁移成功，批量项目恢复负责人和成员绑定。
- 前端开发服务器能够正常启动，登录页正常渲染；页面级登录验证受滑块验证码限制，需要人工完成验证后继续。

### 2026-06-28 全链路自动审计

- 新增统一 Kratos operation audit middleware，替代字典模块手工审计，避免业务模块重复埋点。
- 根据 Casbin 受保护操作清单自动识别非 GET 写操作；操作日志查询、普通读取和登录白名单不记录业务操作审计。
- 成功和失败写操作均记录模块、动作、资源 ID、HTTP/gRPC operation、方法、路径、IP、User-Agent、Trace ID、耗时和错误信息。
- 请求摘要继续通过统一脱敏器处理；超过 16 KiB 的请求只记录 `truncated` 和原始大小，不保存可能包含敏感内容的截断文本。
- 审计写入失败采用降级策略：记录服务错误日志，但不改变原业务请求结果。
- 增加成功、失败、读取跳过、审计存储故障降级和超大敏感请求测试，完整 `go test ./app/platform/admin/...` 通过。
- 真实 HTTP 验证已完成：同一字典创建接口的 200 成功和 409 冲突均生成审计记录，方法、路径、IP、User-Agent、错误信息和脱敏请求摘要正确。
