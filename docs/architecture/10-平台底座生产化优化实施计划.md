# 平台底座生产化优化实施计划

## 1. 目标

本计划用于把当前多租户管理后台从“功能可演示”推进到“权限边界可验证、失败可恢复、生产配置可守门、质量可持续”的项目开发底座。

实施顺序遵循安全性和数据一致性优先于功能扩展的原则。业务套餐能力扩展、通用数据权限覆盖和服务拆分在本轮核心链路稳定后继续推进。

## 2. 优先级

### P0：授权闭环与租户开通一致性

- 普通租户角色的菜单按钮权限必须参与后端接口鉴权，不能只控制前端显示。
- 平台管理员继续由 Casbin 平台策略授权；普通租户角色以数据库中的用户、角色、菜单关系为权限事实源。
- 租户开通后同步授权策略失败时，必须清理策略并回滚已初始化的租户、部门、角色、用户和套餐绑定。
- 跨租户访问必须在授权层和数据层同时拒绝。

验收：

- 普通角色只能访问角色已绑定 `auth_code` 的接口。
- 删除角色按钮权限后，相同用户再次请求立即被拒绝。
- 普通租户用户不能访问平台控制面接口。
- 模拟策略同步失败后，不留下可登录的半成品租户。

### P1：缓存一致性与生产安全门禁

- 套餐变更、租户换套餐、菜单权限变更后，权限缓存必须可靠失效。
- 租户角色、角色菜单和用户角色关系变更后，租户授权快照缓存必须可靠失效。
- Redis 短暂故障不能让权限撤销长期失效；失效操作需要重试和可观测记录。
- 生产镜像必须显式启用生产环境校验，禁止弱口令、开发密钥和缺失的安全中间件配置启动。
- 提供可直接校验的生产配置样例。

验收：

- 权限撤销后，缓存命中场景也不能继续放行。
- Redis 故障恢复后，待处理的缓存失效任务能够自动重试成功。
- 使用开发配置启动生产镜像时进程拒绝启动。

### P2：质量门禁与真实链路验证

- 后端覆盖授权、跨租户隔离、开通补偿和缓存失效测试。
- 前端覆盖普通租户角色登录、菜单可见性和接口拒绝行为。
- Mock 数据同时包含平台管理员、租户管理员、普通角色和跨租户验证数据。
- 根仓库提供统一的后端测试、前端类型检查和关键 E2E 执行入口。

验收：

- 后端全量测试通过。
- 前端类型检查和构建通过。
- 管理后台管理员与普通角色关键流程 E2E 通过。
- Mock 初始化可重复执行并完成内置数据校验。

### P3：底座能力扩展

- 业务套餐从菜单集合扩展为菜单、接口权限、资源额度和功能开关的版本化能力包。
- 通用数据权限从用户查询扩展到项目、审计、字典、参数等租户资源。
- 增加通知中心、文件中心、Webhook、配额计量、幂等和分布式追踪等通用底座能力。
- 达到独立部署和数据生命周期条件后，再拆分新的 Kratos 服务。

## 3. 本轮实施记录

| 日期 | 优先级 | 调整 | 状态 |
| --- | --- | --- | --- |
| 2026-07-05 | P0 | 普通角色菜单按钮权限接入后端授权器 | 已验证 |
| 2026-07-05 | P0 | 租户开通策略失败补偿与初始化数据回滚 | 已验证 |
| 2026-07-05 | P2 | 普通角色授权单元测试及开通回滚仓储测试 | 已验证 |
| 2026-07-05 | P0 | 普通角色权限与租户当前套餐权限取交集 | 已验证 |
| 2026-07-05 | P1 | 缓存版本刷新三次重试，失败后实例内安全旁路 | 已验证 |
| 2026-07-05 | P1 | 生产镜像强制生产环境校验并补全生产配置样例 | 已验证 |
| 2026-07-05 | P2 | 修复 SSE 重复关闭竞态并通过 race 测试 | 已验证 |
| 2026-07-05 | P2 | 扩充普通角色 Mock 权限并完成真实接口验证 | 已验证 |
| 2026-07-05 | P2 | 普通角色前端 E2E 用例 | 已实现，受执行沙箱限制待运行 |
| 2026-07-06 | P1 | 多实例权限缓存失效持久化任务及 worker 重试 | 已验证 |
| 2026-07-10 | P0 | 后端测试基线提交，只包含平台底座测试文件 | 已验证 |
| 2026-07-10 | P0 | 模块接入规范与 ADR 索引固化 | 已实现 |
| 2026-07-10 | P1 | 租户、租户套餐、权限缓存、角色授权和参数上下文补测 | 已验证 |
| 2026-07-14 | P2 | 根仓库真实链路验收脚本，串联迁移、Mock 幂等校验和管理后台 E2E | 已实现 |
| 2026-07-15 | P2 | 根仓库静态质量门禁复验，覆盖后端平台测试、前端 typecheck 和 build | 已验证 |
| 2026-07-15 | P1 | 异步任务健康汇总接口与管理后台可观测入口 | 已验证 |
| 2026-07-15 | P1 | 异步任务健康等级和结构化告警项 | 已验证 |
| 2026-07-15 | P2 | 普通租户角色数据库授权路径 benchmark 基线 | 已验证 |
| 2026-07-16 | P2 | 平台核心包覆盖率门禁，防止测试覆盖退化 | 已验证 |
| 2026-07-16 | P3 | 租户业务套餐扩展为菜单、接口能力、功能开关和资源额度的版本化能力包骨架 | 已验证 |
| 2026-07-16 | P3 | 租户业务套餐 `api_permissions` 接入普通租户角色运行时接口授权 | 已验证 |
| 2026-07-17 | P3 | 当前租户资源额度计量表、查询、检查、占用、释放和超额拒绝闭环 | 已验证 |
| 2026-07-17 | P3 | 当前租户资源额度 `consume/release` 业务幂等键闭环 | 已验证 |
| 2026-07-17 | P3 | 资源额度业务预留封装和 readiness 指标出口 | 已验证 |
| 2026-07-17 | P3 | 资源额度健康巡检脚本和真实后端门禁接入 | 已验证 |
| 2026-07-17 | P3 | 管理后台路由级 `feature_flags` 守卫 | 已验证 |
| 2026-07-17 | P3 | 项目管理接入 `projects` 资源额度创建预留和删除释放链路 | 已验证 |
| 2026-07-17 | P3 | 租户内数据权限扩展到项目管理 owner/member 资源边界 | 已验证 |
| 2026-07-17 | P3 | 租户内数据权限扩展到操作日志和登录日志查询边界 | 已验证 |
| 2026-07-17 | P3 | 字典和参数配置资源数据权限边界复核与分类测试 | 已验证 |
| 2026-07-17 | P3 | 通用幂等内核和 S3-compatible 对象存储内核 | 已验证 |
| 2026-07-17 | P3 | 文件中心后端 MVP：上传会话、文件元数据、S3 预签名、确认和删除 | 已验证 |
| 2026-07-18 | P3 | 文件中心 mock 菜单权限、租户套餐能力和跨租户文件对象数据 | 已验证 |
| 2026-07-18 | P3 | 文件中心多存储渠道：S3-compatible、本地存储、存储渠道管理后台和默认渠道选择 | 已验证 |
| 2026-07-18 | P3 | 文件中心闭环：额度检查、确认占用、删除释放、访问日志和管理后台文件详情 | 已验证 |

## 4. 架构约束

- 前端菜单隐藏不是安全边界，所有受保护操作必须由后端授权器判定。
- 菜单 `auth_code` 必须使用生成契约中的完整操作名，不允许使用页面路径代替接口权限。
- 普通角色权限以数据库关系为事实源，避免 Casbin 和业务表双写造成漂移；Casbin 保留平台控制面和超级管理员策略。
- 任何跨数据库与策略存储的写入都必须具备补偿、重试或持久化任务机制。
- 权限撤销优先保证正确性。缓存不可用时允许降级读取数据库，不允许继续信任无法确认版本的旧缓存。

## 5. 后续复盘要求

每完成一个优先级，必须在本文件更新状态，并记录：

- 实际变更文件和数据迁移；
- 自动化测试与真实环境验证命令；
- 未解决风险及进入下一阶段的条件；
- 对 API、数据模型、Mock 和运维配置的兼容性影响。

## 6. 2026-07-05 验证结果

- `go test ./...`：通过。
- `go test -race ./pkg/kratos/transport/sse ./tests/integration/api`：通过。
- `pnpm --filter @vben/web-antd-admin typecheck`：通过。
- `pnpm --filter @vben/web-antd-admin build`：通过；第三方 `@vueuse/core` 存在 Rolldown 注解警告，不影响产物。
- Mock 迁移和初始化：通过，生成 8 个租户、39 个用户、22 个菜单及权限组、审计、字典、参数和异步任务数据。
- 普通角色 `jack` 真实 HTTP 验证：用户列表返回 200，租户列表和删除用户返回 403。
- Playwright E2E：管理员治理页面和普通租户角色权限场景均已通过。
- 权限菜单和租户套餐变更已调整为先持久化缓存失效任务，再尝试同步递增 Redis 版本；Redis 故障时实例继续旁路缓存并等待异步任务重试。

## 7. 未完成风险

- 权限缓存失效已接入先持久化、后刷新 Redis 的失效意图机制，可覆盖 Redis 暂时不可用和多实例重试。生产环境仍需要对持久化失败日志、连续重试失败任务和 Redis 版本刷新失败配置集中告警。
- 普通角色权限已增加实例级租户授权快照缓存，缓存键绑定菜单版本、租户套餐版本和租户授权版本。仍需要在真实 Redis 与多实例环境中补充压测、命中率和失效延迟观测。
- 租户业务套餐已从菜单和按钮权限扩展为可携带接口能力、功能开关和资源额度的版本化能力包；`api_permissions` 已接入普通租户角色运行时接口授权，`resource_quotas` 已接入当前租户额度查询、检查、占用、释放和超额拒绝。下一步需要把功能开关接入前端全局消费，并为真实业务模块提供统一配额接入封装。

## 8. 2026-07-06 验证结果

- `system.permission_cache_invalidate` handler、合法 scope 和租户 ID 校验测试通过。
- Redis 不可达时，实例内缓存旁路和数据库失效任务持久化测试通过。
- Wire 依赖注入重新生成，服务入口编译通过。
- `go test ./...` 全量通过。
- MySQL/Redis 真实迁移和 Mock 回归通过。
- 普通角色 `jack` 真实 HTTP 验证：用户列表返回 200，租户列表和删除用户返回 403。
- 持久化权限缓存失效任务真实执行成功，Redis 全局菜单版本从 19 递增到 20。
- Playwright E2E 两个场景通过，包含管理员治理页面和普通角色菜单权限。

## 9. 2026-07-10 验证结果

- `backend-service` 测试基线提交：`9cfe103 test(platform): strengthen foundation authorization coverage`。
- `GOCACHE=/private/tmp/avmc-go-cache go test ./...`：通过。
- `GOCACHE=/private/tmp/avmc-go-cache go test ./... -covermode=atomic -coverprofile=/private/tmp/avmc-backend-cover-baseline.out`：通过。
- 原始全仓覆盖率：`3.7%`。
- 过滤 generated/cmd/conf 后有效覆盖率：`31.4%`。
- `platform_admin_internal` 有效覆盖率：`42.4%`。
- 本轮测试覆盖租户开通补偿、租户生命周期、菜单权限组、权限缓存刷新任务、角色菜单授权回显、当前租户参数上下文和认证/鉴权中间件边界。

## 10. 2026-07-14 P2 质量门禁入口

- 静态门禁：`./scripts/verify-foundation.sh`，覆盖 Proto build、Buf lint 基线、平台后端测试、后端 diff 格式、前端 Sass 选择、管理后台 typecheck 和 build。
- 覆盖率门禁：`./scripts/check-platform-coverage.sh`，默认要求平台核心包总覆盖率不低于 `45.0%`，可通过 `PLATFORM_ADMIN_COVERAGE_MIN` 上调阈值。
- 后端真实链路门禁：`./scripts/verify-foundation-live-backend.sh`，要求本机 MySQL 和 Redis 可用，覆盖平台管理后台迁移、Mock 初始化、Mock 再次初始化并比较 `verified` 校验输出、Redis 授权缓存版本集成测试、服务 readiness 和授权缓存健康门禁。
- 完整真实链路门禁：`./scripts/verify-foundation-live.sh`，先执行后端真实链路门禁，再执行关闭 Nitro Mock 的管理后台 Playwright E2E，要求本机 MySQL、Redis、前端依赖和浏览器运行环境可用。
- 授权缓存健康门禁：`./scripts/check-authorization-cache-health.sh`，检查 `/health/ready` 的 `authorization_cache` 指标，默认要求 `bypass_rate <= 0.05`，可通过 `AUTHZ_CACHE_MAX_BYPASS_RATE` 和 `AUTHZ_CACHE_MIN_HIT_RATE` 调整阈值。
- Mock 校验由 `app/platform/admin/cmd/mock` 内置，覆盖演示租户、用户、菜单、套餐、租户套餐绑定、角色、岗位、部门、项目、字典、参数、操作日志、登录日志、异步任务和文件中心对象的最小可用数据。
- 该入口用于 P2 后续验收，避免只运行前端类型检查或后端单元测试就误判真实后台链路完成。

验证结果：

- `./scripts/verify-foundation.sh`：通过。
- `./scripts/verify-foundation-live.sh`：通过；迁移成功，Mock 连续两次校验输出一致，Playwright E2E 两个场景通过。
- 2026-07-15 复验 `./scripts/verify-foundation.sh`：通过；覆盖 Buf build、Buf lint 基线、平台后端测试、后端 diff 检查、Sass 编译器选择、管理后台 typecheck 和生产 build。
- 2026-07-16 新增 `./scripts/check-platform-coverage.sh`：通过；平台核心包覆盖率 `45.8%`，默认门禁阈值 `45.0%`。
- 2026-07-16 增强 `./scripts/verify-foundation-live-backend.sh`：通过；迁移成功，Mock 连续两次校验输出一致，Redis 授权缓存版本集成测试通过，后端服务 readiness 和授权缓存健康门禁通过。
- 2026-07-16 复验 `./scripts/verify-foundation-live.sh`：后端真实链路通过；前端 Playwright E2E 在当前 Codex 沙箱内被 Chromium MachPort 权限拒绝阻断，未进入业务页面断言。
- 2026-07-16 新增 `./scripts/check-authorization-cache-health.sh`：通过；真实后端服务 `/health/ready` 返回授权缓存指标，当前 `hit_rate=0`、`bypass_rate=0`。
- `pnpm --filter @vben/web-antd-admin typecheck`：通过。
- 管理后台 E2E 用例补充等待菜单加载态消失，避免深链接跳转时把路由菜单初始化误判为业务数据缺失。
- 管理后台 E2E 用例补充页面数据有限重试，覆盖真实链路冷启动时首个列表接口瞬时 `context deadline exceeded` 后页面停留空态的问题。
- 当前 Codex 沙箱内直接运行 Chromium 会触发 macOS MachPort 权限拒绝；Playwright E2E 需要在允许浏览器进程的环境中运行。

## 11. 2026-07-15 P1 异步任务可观测入口

- 后端新增 `GET /admin/v1/async-tasks:stats`，返回任务总数、状态分布、待执行超时、运行租约过期、失败任务和重试压力。
- 统计接口纳入平台控制面权限，普通租户角色不应获得该接口授权。
- 管理后台 `/system/async-task` 增加任务健康汇总区，取消或重试任务后同步刷新统计。
- 健康汇总进一步返回统一健康等级和结构化告警项：积压或失败为 `warning`，租约过期或重试压力为 `critical`。
- 本轮保持任务执行状态机不变，不开放任意创建任务 API。
- 生产配置门禁补充 `Load` 级别回归测试，覆盖生产环境加载本地默认配置失败，以及通过必需环境变量覆盖后成功。
- 普通租户角色授权路径补充 benchmark 基线，当前 SQLite 单次允许判定约 `434316 ns/op`、`66855 B/op`、`1070 allocs/op`，后续授权快照缓存优化必须以该基线为对照。
- 普通租户角色授权路径增加实例级快照缓存：命中前必须读取 Redis 中的菜单、套餐和授权三类版本；Redis 不可达、版本不可确认或版本刷新失败时回退数据库实时判定。
- 角色新增、角色更新、角色删除、用户创建带角色、用户更新角色后，会持久化 `tenant_authorization` 缓存失效任务，清理当前实例租户授权快照并尝试递增 Redis 授权版本。
- 授权快照缓存增加实例级统计计数，覆盖命中、未命中、写入、安全旁路、过期、清理和 worker 失效执行次数，为后续接入指标系统提供稳定采集点。
- 后端真实链路脚本 `./scripts/verify-foundation-live-backend.sh` 增加 Redis 版本键集成测试，验证授权快照在三版本键可确认时命中，并在租户授权版本递增后立即失效；完整真实链路脚本 `./scripts/verify-foundation-live.sh` 复用该后端门禁后再执行管理后台 E2E。
- 异步任务健康汇总增加权限缓存失效专用告警 `permission_cache_failed` 和 `permission_cache_retry_pressure`，并支持通过环境变量配置各类告警触发阈值。
- `/health/ready` 支持可选 `details` 输出，平台管理后台暴露 `authorization_cache` 统计，包括命中、未命中、安全旁路、过期、清理、失效执行次数，以及 `hit_rate` 和 `bypass_rate`。
- 根仓库新增授权缓存健康检查脚本，可在预发、生产发布后或巡检任务中对 `authorization_cache.bypass_rate` 和 `authorization_cache.hit_rate` 设置阈值并自动失败。

## 12. 2026-07-16 P3 租户业务套餐能力包骨架

- `MenuPermissionGroup` 和 `MenuPermissionGroupVersion` 增加 `api_permissions`、`feature_flags` 和 `resource_quotas`，保持原 `menu_ids` 兼容。
- 套餐创建会生成包含菜单、接口能力、功能开关和资源额度的初始版本快照。
- 套餐编辑时，菜单或能力字段发生变化会自动发布新版本；仅元数据变化不产生新版本。
- 显式发布版本支持设置能力字段；旧调用方只传菜单时会继承当前套餐能力，避免误清空功能开关和额度。
- 回滚版本会复制历史版本的完整能力快照，而不只回滚菜单。
- Mock 演示套餐增加接口能力、功能开关和资源额度，重复初始化仍保持幂等。
- 管理后台租户业务套餐表单和发布弹窗支持编辑接口能力、功能开关 JSON 和资源额度 JSON。

验证结果：

- `GOCACHE=/private/tmp/avmc-go-cache go test ./app/platform/admin/internal/data -run 'TestMenuPermissionGroup'`：通过。
- `GOCACHE=/private/tmp/avmc-go-cache go test ./app/platform/admin/internal/data ./app/platform/admin/internal/service ./app/platform/admin/internal/biz`：通过。
- `GOCACHE=/private/tmp/avmc-go-cache go test ./app/platform/admin/...`：通过。
- `GOCACHE=/private/tmp/avmc-go-cache go test ./...`：通过。
- `buf build`：通过。
- `./scripts/check-buf-lint-baseline.sh`：通过。
- `pnpm --filter @vben/web-antd-admin typecheck`：通过。

剩余风险：

- `api_permissions` 已接入普通租户角色运行时接口授权：套餐或绑定版本声明接口能力时，角色菜单命中后仍必须匹配当前 operation；空接口能力列表按历史套餐兼容处理。
- `resource_quotas` 已从额度定义和版本快照推进到后端计量闭环：当前租户可查询额度、检查本次占用、原子占用额度、释放额度，并在超额时返回 `RESOURCE_QUOTA_EXCEEDED` forbidden reason。
- `feature_flags` 已提供后端当前租户运行时读取入口，并接入管理后台 `platform-capability` store、`usePlatformCapability()`、`FeatureGate` 组件和路由级守卫；页面和后续业务模块可通过 `hasFeature()`、`hasApiPermission()`、`getQuotaLimit()`、`checkQuota()`、`consumeQuota()`、`releaseQuota()`、`canConsumeCached()` 读取和操作运行时能力。真实业务页面可在 route meta 声明 `featureFlags` 和 `featureFlagMode`，未命中时跳转 403。

2026-07-17 补充：

- 后端新增 `GET /admin/v1/current-tenant/capabilities`，只从认证上下文读取当前租户，不接受前端传入租户 ID。
- 响应聚合当前租户启用套餐和固定版本中的 `api_permissions`、`feature_flags`、`resource_quotas`、`group_ids` 和绑定版本信息。
- 聚合语义：接口能力取并集；功能开关按 `true` 优先合并，所有套餐均为 `false` 时保留 `false`；资源额度按同名 key 最大值合并。
- 固定版本优先于套餐当前版本；禁用绑定和禁用套餐不参与运行时能力聚合。
- 新接口纳入受保护接口列表，但归类为当前租户自助读取能力，不归入平台控制面操作。
- 后端新增租户资源额度使用量表 `tenant_resource_quota_usages`，按 `(tenant_id, resource_key)` 唯一记录当前已占用量，额度上限仍以租户套餐运行时能力聚合结果为事实源，不在使用量表重复存储。
- 后端新增租户资源额度操作幂等流水表 `tenant_resource_quota_operations`，按 `(tenant_id, operation_type, idempotency_key)` 唯一记录首次占用或释放后的使用量快照；重复请求会回放首次结果，同一幂等键复用到不同资源键或数量时返回 `RESOURCE_QUOTA_IDEMPOTENCY_CONFLICT`。
- 当前租户资源额度接口统一挂载在 `TenantPermissionService`：`GET /admin/v1/current-tenant/resource-quotas`、`GET /admin/v1/current-tenant/resource-quotas/{resource_key}:check`、`POST /admin/v1/current-tenant/resource-quotas/{resource_key}:consume`、`POST /admin/v1/current-tenant/resource-quotas/{resource_key}:release`。
- 配额键采用分段小写格式，例如 `projects`、`ai.tokens`；套餐未声明的资源键按兼容策略视为不限额，套餐声明为 `0` 时表示不可占用。
- 占用流程在仓储层事务内先检查幂等流水，再锁定当前租户资源键记录，先检查再更新；释放流程不会把使用量扣为负数。
- 后端 `ResourceQuotaUsecase` 增加 `ReserveCurrent()` 业务预留封装，要求调用方提供业务幂等键，并自动派生释放幂等键，后续业务模块不应直接散落组合 `ConsumeCurrent()` 和 `ReleaseCurrent()`。
- `ReserveCurrent()` 预留结果增加幂等回放标记，业务方在保存失败时只补偿首次预留，避免重试或唯一键冲突场景误释放已有额度。
- 项目管理成为首个真实接入资源额度的底座模块：创建项目会按当前租户 `projects` 额度预留 1 个资源，额度不足时在写库前拒绝；创建写库失败会释放首次预留；删除项目成功后按 `project:delete:<id>` 幂等键释放 1 个资源。
- 租户内数据权限解析从用户仓储抽为可复用能力，项目管理成为第二个受控资源：非全部数据范围用户只能看到负责人或成员落在其可访问用户集合内的项目；项目详情、列表和计数使用同一范围，更新和删除通过 usecase 的详情前置校验继承该边界。
- 操作日志和登录日志查询接入同一数据权限解析：非全部数据范围用户只能查看可访问用户集合内的 `operator_id` 或 `user_id` 对应日志；无登录用户的系统上下文保持不受限，避免中间件写入和系统任务写入被误拦截。
- 字典和参数配置资源完成边界复核：字典是租户共享配置，依赖 `tenant_id` 隔离和操作授权，不按部门数据范围裁剪；参数定义和指定租户参数读写保持平台控制面分类，当前租户参数读写保持租户自助配置分类。
- P3 通用底座能力开始按内核优先方式展开：新增通用幂等执行模型和 S3-compatible 对象存储抽象，文件中心、通知中心和 Webhook 后续统一接入该内核，避免业务模块直接绑定具体云厂商 SDK。
- 文件中心后端 MVP 已接入 `platform/admin`：新增文件契约、元数据表、上传会话、S3-compatible 预签名上传/下载、上传确认、删除和权限保护；文件元数据会记录存储渠道快照，避免后续默认渠道切换影响历史文件。
- 文件中心已升级为多存储渠道模型：新增 `StorageProviderService`、`storage_providers` 表、默认渠道选择、渠道连通性测试和前端 `/system/storage-provider` 管理页面；支持 S3-compatible 和本地存储两类渠道，技术中台可统一配置、启停、设为默认并查看健康状态。
- 本地存储渠道用于开发、私有化轻量部署和离线演示：服务端上传入口为 `POST /admin/v1/files/{id}:content`，对象写入受控基础目录，禁止路径穿越；该渠道不支持客户端预签名 PUT。
- 文件中心完成资源额度闭环：创建上传会话前检查 `files` 和 `storage.bytes`，确认上传后按文件 ID 派生幂等键占用文件数和存储容量，删除文件后按删除幂等键释放额度。
- 文件访问日志已接入：下载和删除动作写入 `file_access_logs`，新增 `GET /admin/v1/files/{file_id}/access-logs`，管理后台 `/system/file-center` 支持详情和访问记录查看。
- Mock 数据已接入文件中心：新增 `/system/file-center` 菜单和列表、详情、上传、确认、下载、删除按钮权限；`demo-full-admin` 套餐增加 `platform.admin.file.manage`、`file_center` 功能开关、`files` 和 `storage.bytes` 资源额度；租户 1 生成合同、导入、公开 Logo 和待确认上传 4 条文件对象，租户 2 生成发票和归档 2 条文件对象，用于后台查看跨租户隔离。
- Mock 数据已接入存储渠道管理：默认本地渠道 `mock-local-default` 指向 `/private/tmp/avmc-platform-files`，示例 S3-compatible 渠道 `mock-s3-compatible` 用于管理页查看和后续 MinIO/R2/OSS/COS 接入演示；Mock 自检要求存储渠道数量不少于 2。
- `/health/ready` details 增加 `resource_quota` 指标，输出 `consumes`、`releases`、`quota_exceeded`、`idempotency_conflicts`、`mutations`、`exceeded_rate` 和 `conflict_rate`，用于生产巡检和指标采集。
- 根仓库新增 `scripts/check-resource-quota-health.sh`，可检查 readiness details 中的资源额度指标结构、计数一致性、超额率和幂等冲突率；`scripts/verify-foundation-live-backend.sh` 已在真实后端服务 readiness 阶段串联该脚本。
- 当前租户配额接口纳入受保护接口列表，但归类为当前租户自助能力，不归入平台控制面操作。
- 剩余风险：除项目管理外的后续业务模块仍需按资源键逐一接入 `ReserveCurrent()` 封装；部门、岗位和后续业务实体等具备明确组织归属的资源仍需继续接入数据权限；配额指标已具备 readiness 出口和本地巡检门禁，但尚未接入集中告警系统。
- 前端新增当前租户能力 API wrapper、`platform-capability` Pinia store、`usePlatformCapability()` hook 和 `FeatureGate` 组件；登录、刷新用户信息和路由守卫补拉用户信息时会刷新当前租户能力，能力接口失败不阻断登录主链路。
- 管理后台路由守卫支持 `meta.featureFlags` 和 `meta.featureFlagMode`，动态路由生成完成后会基于当前租户套餐能力做页面级拦截；未声明 feature flag 的既有页面行为不变。
- 前端配额封装已从只读缓存推进到可操作方法：`checkQuota()` 透传后端实时校验，`consumeQuota()` 和 `releaseQuota()` 支持传入可选业务幂等键，调用后端接口后会同步更新本地 `quotaUsages`。

验证结果：

- `buf build`：通过。
- `./scripts/check-buf-lint-baseline.sh`：通过，未扩大现有 Buf lint baseline 债务。
- `GOCACHE=/private/tmp/avmc-go-cache go test ./app/platform/admin/internal/data -run 'TestMenuPermissionGroupTenantCapabilities|TestTenantRoleAuthorizer'`：通过，覆盖当前租户能力聚合、固定版本覆盖当前套餐、禁用套餐忽略和接口能力授权链路。
- `GOCACHE=/private/tmp/avmc-go-cache go test ./app/platform/admin/internal/service ./app/platform/admin/internal/biz ./app/platform/admin/internal/authzpolicy`：通过。
- `GOCACHE=/private/tmp/avmc-go-cache go test ./...`：通过。
- `GOCACHE=/private/tmp/avmc-go-cache go test ./... -covermode=atomic -coverprofile=/private/tmp/avmc-backend-cover.out`：通过，总语句覆盖率 `3.9%`；平台重点包覆盖率为 `authzpolicy 86.7%`、`biz 50.7%`、`data 51.9%`、`service 17.0%`。
- `GOCACHE=/private/tmp/avmc-go-cache go test ./app/platform/admin/internal/data -run 'TestTenantRoleAuthorizer'`：通过，覆盖套餐接口能力声明和固定版本接口能力覆盖当前套餐的运行时授权。
- `GOCACHE=/private/tmp/avmc-go-cache go test ./app/platform/admin/internal/biz ./app/platform/admin/internal/data ./app/platform/admin/internal/service ./app/platform/admin/internal/authzpolicy ./app/platform/admin/internal/server`：通过，覆盖资源额度 usecase、仓储事务、服务接口和鉴权分类。
- `GOCACHE=/private/tmp/avmc-go-cache go test ./app/platform/admin/internal/data -run '^$' -bench '^BenchmarkTenantRoleAuthorizerEnforceAllowed$' -benchtime=100x -benchmem`：通过；当前无 Redis 命中数据库路径约 `1245471 ns/op`、`78156 B/op`、`1258 allocs/op`。
- `GOCACHE=/private/tmp/avmc-go-cache go test ./app/platform/admin/internal/data ./app/platform/admin/internal/service ./app/platform/admin/internal/biz ./app/platform/admin/internal/authzpolicy`：通过。
- `GOCACHE=/private/tmp/avmc-go-cache go test ./app/platform/admin/internal/runtimeconfig`：通过。
- `GOCACHE=/private/tmp/avmc-go-cache go test ./app/platform/admin/internal/data -run '^$' -bench '^BenchmarkTenantRoleAuthorizerEnforceAllowed$' -benchtime=100x`：通过。
- `GOCACHE=/private/tmp/avmc-go-cache go test ./app/platform/admin/internal/biz ./app/platform/admin/internal/data ./app/platform/admin/internal/service ./app/platform/admin/internal/authzpolicy`：通过。
- `GOCACHE=/private/tmp/avmc-go-cache go test ./...`：通过。
- `PATH=/Users/jayden/go/bin:$PATH buf build`：通过。
- `PATH=/Users/jayden/go/bin:$PATH GOCACHE=/private/tmp/avmc-go-cache make api && make ent && make doc`：通过，生成 API、Ent 和 OpenAPI 文档。
- `GOCACHE=/private/tmp/avmc-go-cache go test ./app/platform/admin/internal/biz ./app/platform/admin/internal/data ./app/platform/admin/internal/service`：通过，覆盖资源额度幂等键校验、重复占用/释放回放和冲突检测。
- `GOCACHE=/private/tmp/avmc-go-cache go test ./app/platform/admin/internal/biz ./app/platform/admin/internal/data ./pkg/health`：通过，覆盖资源额度业务预留封装、配额 repo 指标计数和 readiness details 指标出口。
- `GOCACHE=/private/tmp/avmc-go-cache go test ./app/platform/admin/internal/data`：通过，覆盖用户数据权限旧行为和项目 owner/member 数据权限边界。
- `GOCACHE=/private/tmp/avmc-go-cache go test ./app/platform/admin/internal/data`：通过，覆盖操作日志 `operator_id` 与登录日志 `user_id` 的数据权限边界。
- `GOCACHE=/private/tmp/avmc-go-cache go test ./app/platform/admin/internal/authzpolicy`：通过，覆盖字典租户数据面、参数平台控制面和当前租户参数自助配置分类。
- `GOCACHE=/private/tmp/avmc-go-cache go test ./pkg/idempotency ./pkg/objectstorage`：通过，覆盖通用幂等回放/冲突/失败重试和 S3-compatible 预签名、上传签名、公开 URL 生成。
- `GOCACHE=/private/tmp/avmc-go-cache go test ./app/platform/admin/internal/biz ./app/platform/admin/internal/data ./app/platform/admin/internal/service ./app/platform/admin/internal/server ./app/platform/admin/internal/authzpolicy ./app/platform/admin/cmd/server ./pkg/objectstorage`：通过，覆盖文件中心后端 MVP 编译、usecase 行为和权限分类。
- `GOCACHE=/private/tmp/avmc-go-cache go test ./app/platform/admin/internal/authzpolicy ./app/platform/admin/cmd/mock`：通过，覆盖文件中心权限分类和 mock 程序编译。
- `GOCACHE=/private/tmp/avmc-go-cache go run ./cmd/mock -conf ./configs -migrate`：通过，补齐 `file_objects` schema 并初始化文件中心 mock 数据。
- `GOCACHE=/private/tmp/avmc-go-cache go test ./pkg/objectstorage ./app/platform/admin/internal/authzpolicy ./app/platform/admin/internal/biz ./app/platform/admin/internal/data ./app/platform/admin/internal/service ./app/platform/admin/internal/server ./app/platform/admin/cmd/server`：通过，覆盖本地对象存储、多渠道文件中心、存储渠道服务和权限分类。
- `GOCACHE=/private/tmp/avmc-go-cache go test ./...`：通过。
- `PATH=/Users/jayden/go/bin:$PATH buf build`：通过。
- `PATH=/Users/jayden/go/bin:$PATH ./scripts/check-buf-lint-baseline.sh`：通过。
- `GOCACHE=/private/tmp/avmc-go-cache go run ./cmd/mock -conf ./configs -migrate`：通过，新增 `storage_providers`、`file_access_logs` schema 并初始化文件中心、存储渠道和访问日志 mock 数据；校验输出包含 `tenant 1 file access logs count=7`、`tenant 2 file access logs count=4`。
- `GOCACHE=/private/tmp/avmc-go-cache go run ./cmd/mock -conf ./configs`：通过，幂等校验输出保持 `storage providers count=2`、`tenant 1 file access logs count=7`、`tenant 2 file access logs count=4`。
- `pnpm -F @vben/web-antd-admin run typecheck`：通过。
- `pnpm -F @vben/web-antd-admin run build`：通过；仍存在既有第三方 `@vueuse/core` Rolldown 注解警告，不影响构建产物。
- `GOCACHE=/private/tmp/avmc-go-cache go run ./cmd/mock -conf ./configs`：通过，重复执行后校验 `tenant 1 file objects count=4`、`tenant 2 file objects count=2`。
- `bash -n scripts/check-resource-quota-health.sh scripts/verify-foundation-live-backend.sh`：通过。
- `HEALTH_BODY='...' scripts/check-resource-quota-health.sh`：健康样例通过；幂等冲突率超阈值样例按预期失败。
- `pnpm -F @vben/web-antd-admin run typecheck`：通过。
- `pnpm -F @vben/web-antd-admin run build`：通过；仍有 `@vueuse/core` pure annotation 依赖警告，不影响构建产物。
- `pnpm -F @vben/web-antd-admin run typecheck`：通过。
- `pnpm -F @vben/web-antd-admin run build`：通过；仍有第三方 `@vueuse/core` Rolldown pure annotation 警告，不影响产物。
- `pnpm -F @vben/web-antd-admin run typecheck`：通过，覆盖 `usePlatformCapability()`、`FeatureGate` 和配额操作封装。
- `pnpm -F @vben/web-antd-admin run build`：通过；仍有第三方 `@vueuse/core` Rolldown pure annotation 警告，不影响产物。
- `pnpm -F @vben/web-antd-admin run typecheck`：通过，覆盖路由级 `featureFlags` 守卫和 `/403` 核心路由。
- `pnpm -F @vben/web-antd-admin run build`：通过；仍有第三方 `@vueuse/core` Rolldown pure annotation 警告，不影响产物。
- `GOCACHE=/private/tmp/avmc-go-cache go test ./... -covermode=atomic -coverprofile=/private/tmp/avmc-backend-cover.out`：通过，总语句覆盖率 `3.8%`；平台重点包覆盖率为 `authzpolicy 86.1%`、`biz 48.2%`、`data 49.0%`、`runtimeconfig 80.1%`、`server 41.7%`。
- `GOCACHE=/private/tmp/avmc-go-cache go test ./app/platform/admin/internal/data -run '^$' -bench '^BenchmarkTenantRoleAuthorizerEnforceAllowed$' -benchtime=100x -benchmem`：通过，当前无 Redis 命中数据库路径约 `379558 ns/op`、`66857 B/op`、`1070 allocs/op`。
- `GOCACHE=/private/tmp/avmc-go-cache go test ./...`：通过。
- `GOCACHE=/private/tmp/avmc-go-cache go test ./... -covermode=atomic -coverprofile=/private/tmp/avmc-backend-cover-p1-observability.out`：通过，总覆盖率 `3.7%`。
- `pnpm --filter @vben/web-antd-admin typecheck`：通过。
- `GOCACHE=/private/tmp/avmc-go-cache go test ./app/platform/admin/internal/data`：通过，覆盖授权快照缓存旁路和清理统计。
- `GOCACHE=/private/tmp/avmc-go-cache go test -tags=integration ./app/platform/admin/internal/data -run '^TestTenantAuthorizationCacheWithRedisVersions$'`：用于真实 Redis 验证，已接入 live 门禁。
- `GOCACHE=/private/tmp/avmc-go-cache go test ./app/platform/admin/internal/data ./app/platform/admin/internal/biz ./app/platform/admin/internal/service`：通过，覆盖异步任务告警阈值和权限缓存失效专用告警。
- `GOCACHE=/private/tmp/avmc-go-cache go test ./pkg/health ./app/platform/admin/internal/data`：通过，覆盖 readiness details 和授权缓存指标出口。
